# Claude Code 面试问答大全(最终版)

> 整合自 8 份专题手册(CLAUDE.md、源码架构、主循环 Query、上下文 Compact、记忆机制、代码检索、多 Agent、Skill),素材来自小林 coding 的 Claude Code 源码拆解系列。
> 用法：每题按「一句话亮观点 → 展开要点 → 收尾金句」组织；粗体是得分点；文末附数字速查表和 60 秒电梯版。

---

## 目录

1. 基础概念
2. CLAUDE.md(项目记忆)
3. 整体架构与 Agent 工作模式
4. 主循环 Query
5. System Prompt 工程
6. 上下文管理与 Compact 压缩
7. 记忆机制
8. 代码检索:grep vs RAG
9. 多 Agent(SubAgent)
10. Skill 机制
11. 60 秒电梯版 & 数字速查表

---

## 一、基础概念

### Q1:Claude Code 是什么？Agent 和 ChatBot、Copilot 有什么区别？

**Claude Code 是 Anthropic 官方的终端编程 Agent**，能读代码、改文件、跑命令、管 Git。

三者区别：
- ChatBot：一问一答，一次性问答；
- Copilot：代码补全，一次性预测；
- **Agent:「感知-决策-行动」自主循环**——给个目标（如「修这个 bug」)，它自己决定先读哪个文件、跑什么命令、改哪行代码，循环几十轮直到完成。关键是**大模型自己决定下一步**，不是按预定义流程图走。

### Q2：什么是 Harness Engineering（线束工程）?

AI 圈的风向转变：从「拼模型智商」变成「**拼系统求稳**」——与其祈求大模型不产生幻觉，不如用系统约束它的行为、划好赛道。Claude Code 泄漏的 51 万行客户端源码就是最佳教科书：**80% 的代码不是让 AI 更聪明，而是死磕可靠性**。

金句：「模型是发动机，但车能不能安全上路，靠的是刹车、方向盘、安全带。」

### Q3:LLM 的「记忆」是怎么回事？

**LLM 彻底无状态**（金鱼记忆）：每次请求都是「从头看一遍」——system prompt + 历史对话 + 当前问题全塞进去再生成。你以为它记得你，其实是客户端把历史消息一起发了过去。上下文窗口就是这堆消息的最大长度。

记忆分两种：**短期记忆** = 上下文窗口本身（满了靠 compaction);**长期记忆** = 持久化到磁盘、跨会话存活。

---

## 二、CLAUDE.md(项目记忆)

### Q4:CLAUDE.md 是什么？和 README 有什么区别？

本质：项目根目录下固定文件名的 markdown,**每次会话启动自动完整加载进上下文**，作为整个对话的 ground truth——不是可选提示，是默认前提。

与 README 的区别（官方原话）:**README 写给****人****看，CLAUDE.md 写给 agent 看**;Claude 默认不会主动读 README。一句话:**CLAUDE.md 不是文档，是给 Agent 的入职手册/配置**。

### Q5：为什么 CLAUDE.md 写多了反而废？

SFEIR Institute 实测：≤200 行遵守率 ≈92%,>400 行掉到 ≈70%；拆成 5 个 30 行模块文件放 `.claude/rules/` 反而升到 **96%**。

两个原因：
1. **Token 经济**：每次启动完整加载，行数越多越挤压对话和工具结果的空间；
2. **注意力稀释**：规则一多每条权重被摊薄，超 300 行「记不住」成常态。

**黄金线 200 行，推荐 80 行起步。**

### Q6：哪些内容不该写进 CLAUDE.md?

三类负资产直接删：
- **复述型**：整段抄架构文档 → 改为一行指路「详见 docs/architecture.md」;
- **愿望型**:「希望覆盖率 90%」→ 只写当下实际执行的规则（「PR 前必须跑 npm test」是规则，「希望大家多写测试」是 PUA);
- **术语表型**：通用术语 Claude 都懂，团队黑话放 docs/glossary.md。

### Q7：什么样的规则才真正有效？

四个原则:**短、具体、告诉为什么、持续更新**。
- 具体 = 可验证:「TS 文件用 2 空格缩进」能自检,「代码要规范」只能猜；
- **告诉 why 最关键**:「不要写生产库，**因为去年测试清空过 users 表**」→ Claude 知道规则边界，能对 staging 场景做正确判断而非机械执行；
- 持续更新：同一错误犯 **2 次以上**加防御规则；过时规则要删——**「错误的规则比没有规则更糟」**。

### Q8:CLAUDE.md 如何分层组织？

加载机制：从当前目录向上爬到根，逐层读取所有 CLAUDE.md 合并喂模型。三层 + 一套模块化机制：
- 项目根 `CLAUDE.md`：通用约定（技术栈/目录/命令/硬约束）;
- 子目录 `CLAUDE.md`：模块约定，**按需加载**;
- `~/.claude/CLAUDE.md`：全局个人偏好;
- **`.claude/rules/`**：每主题一个 ≤30 行文件，frontmatter 写 `paths: ["**/*.test.ts"]` 实现 **path-scoped** 按需加载。

跨工具：Codex 用 AGENTS.md，规则全写 AGENTS.md,CLAUDE.md 只留一行 `@AGENTS.md` 引用——**一份文件、两个工具、零重复维护**。

工具链:`/init` 生成草稿 → `/memory` 会话中维护 → 复杂改动先切 Plan Mode。注意**规则冲突**:「两条规则矛盾，Claude 可能随便挑一条」,1-2 周 review 一次。

### Q9：推荐什么模板？

6 段式 ≤80 行:Project Overview(2-3 行）/ Commands（常用命令）/ Architecture（三句话指路不复述）/ Conventions（真实在用的约定）/ Hard Constraints（带 why)/ Gotchas(踩坑，价值最高，Claude 无法从代码推断）。

---

## 三、整体架构与 Agent 工作模式

### Q10:Claude Code 的整体架构？

**四层架构**:
| 层 | 职责 |
|---|---|
| 引擎层 | 「大脑」：协调（拼上下文发 API)、分发（找工具执行）、决策（继续/结束）。**不含任何业务逻辑**，新增能力只需加工具 |
| 工具层 | 40+ 工具；统一规范 + **类型系统强制三个安全属性**：是否只读/是否有破坏性/能否并发，漏一个编译不过——「每把刀出厂配好刀鞘」 |
| 服务层 | 共享基础设施：大模型 API、上下文压缩、MCP 协议（水电煤） |
| 安全与治理层 | 一张网罩在所有层上：权限系统、Hook 系统、Bash 安全模块（**语法级分析**命令注入/路径逃逸，不是正则匹配关键词） |

### Q11:Claude Code 用的什么 Agent 框架？是 ReAct 吗？（高频)

**不是 ReAct，是 Tool-Use Loop**。ReAct 的三个问题:①每轮输出显式 Thought 浪费 token(50 轮就是几万）;②应用层要解析文本区分 Thought/Action，格式不标准就崩;③ReAct 是弱模型时代的产物。

Tool-Use Loop 就是一个 `while(true)`：模型返回 `tool_use` → 执行工具塞回结果继续循环；返回 `end_turn` → 跳出。更好的三个原因:
1. **Extended Thinking** 让推理在模型内部完成，不占上下文、不需解析；
2. API **原生支持 tool_use**，不需要正则提取 Action;
3. **end_turn 是天然终止信号**,API 层原语，语义清晰。

金句:**「信任模型的推理能力，把应用层框架做得尽可能简单。」**

### Q12:Plan Mode 是怎么实现的？

不是独立框架，是同一循环里的 `EnterPlanMode`/`ExitPlanMode` **两个工具**——「工具即能力」，引擎层无需特殊处理。三步:①模型判断复杂自主进入（或用户 Shift+Tab);②**权限降为只读**，只能用 Read/Grep/Glob 探索，计划写入 `.claude/plans/`，每 5 轮塞「小纸条」防走神;③ExitPlanMode 需**用户审批**才恢复权限执行。

---

## 四、主循环 Query

### Q13：介绍一下 Claude Code 主循环 Query 的流程？（硬核高频)

**四层调用链**,yield* 接力、事件一路冒泡：
- `ask`:SDK 一次性入口，new QueryEngine 转交；
- `QueryEngine.submitMessage`：管会话状态（消息历史/文件缓存/权限记录）的「记账本」，处理斜杠命令、组装 system prompt;
- `query`:**异步生成器**流式包装层（「边干边吐」是流式+并行工具的前提）;
- `queryLoop`:while(true) 循环本体。

**每轮五步**:①准备消息（压缩是**被动触发**——先原样发，API 返回 prompt_too_long 才压缩重试，同轮只压一次靠 `hasAttemptedReactiveCompact` 防死循环）;②流式调模型，文字块即时 yield 给用户、tool_use 块攒起来;③**看有没有 tool_use 块**决定继续还是结束（`needsFollowUp`);④执行工具;⑤结果作为 user 消息塞回历史，更新 State 进下一轮。

### Q14:Agent 怎么知道该用工具还是该停下来？

简单到出乎意料：**就看返回里有没有 tool_use 块**。有 → 继续；没有 → `return { reason: 'completed' }`。因为「决定下一步做什么」本来就是大模型的工作。

但退出原因有 **17 种**——正常完成只是最小一支，代表性异常:max_turns（轮数封顶止损）、aborted_streaming/aborted_tools(Ctrl+C 两种中断）、prompt_too_long、max_output_tokens_recovery（续写 3 次失败）、stop_hook_prevented（用户钩子拦截）、image_error。**工业级主循环兜的不是正常退出，是十几种异常退出**——1729 行代码 80% 在异常路径。

### Q15：模型还在吐字、工具就开跑了，怎么做到的？

`StreamingToolExecutor` **流式并行**:①模型流式输出时，每识别到一个完整 tool_use 块立刻 `addTool` 丢后台开跑；②模型流完后 `getRemainingResults` 一次性收结果。点菜类比：报一个菜名后厨就先炒，点完时红烧肉已炒一半——**把模型生成时间和工具执行时间高度重叠**。

并行边界：只读工具（Read/Grep/Glob）随便并行；写工具（Edit/Write/Bash）必须串行；属性漏标则 **fail-closed 默认串行**——宁可错杀，绝不并发踩踏。

### Q16：出错的时候主循环怎么兜底？（三个机制)

1. **State 对象显式管理跨轮状态**(messages/turnCount/maxOutputTokensRecoveryCount/hasAttemptedReactiveCompact),不用闭包全局变量——计数器和防重复标志防无限循环，「能调试的状态才是可靠的状态」;
2. **工具跑挂合成假 tool_result**:API 死规定每个 tool_use 必须有配对 tool_result，否则下轮请求被拒收。工具中断/网络断/模型降级时，合成带 `is_error: true` 的假结果塞回（`yieldMissingToolResultBlocks`)——「**先认错，再继续**」，所以 Ctrl+C 后对话还接得上;
3. **输出截断两段式恢复**：首次 8k 截断**静默升档 64k** 重试（用户无感知）;还截断则下轮消息里 **nudge**(「Resume directly — no apology, no recap」）让模型断点续写；**3 次上限**后退出。

金句:**「工业级的容错不是报错弹窗，而是用户根本感觉不到出过错。」「错误恢复优先于优雅。」**

---

## 五、System Prompt 工程

### Q17:Claude Code 的 System Prompt 是怎么构造的？

不是静态文本，是**十几个 Section 动态组装**。值得抄的设计:
1. 角色定位「interactive agent」而非 assistant——暗示主动行动；立刻划红线（不乱编 URL);
2. 安全约束「**先肯定再约束**」：先说可以做（授权安全测试/CTF)，再说不能做（DoS/供应链攻击）——给判断依据而非模糊禁令;
3. 行为准则：改代码前必须先读代码；「少即是多」不做没要求的重构（三行相似代码好过一个过早的抽象）;「先诊断再换方案」不盲目重试也不轻易放弃;
4. 操作安全 = **可逆性 × 影响范围**两维度：本地可逆直接放行，不可逆/影响他人必须确认；**授权一次性、有范围**（防权限蔓延）;
5. 专用工具优先于 Bash（可审查性 + 专用权限检查）;Git 协议（永远新建 commit 不用 --amend，因为 pre-commit hook 失败时 commit 没发生,--amend 会改到不相关的上一个 commit);
6. 输出风格：工具调用间 ≤25 词，最终回复 ≤100 词。

### Q18:System Prompt 组装方式为什么影响费用？（亮点题)

中间有一条分割线 `__SYSTEM_PROMPT_DYNAMIC_BOUNDARY__`:
- **之上**（角色/安全/准则/Git 协议等）对所有用户完全一样 → 全球共享 **Prompt Cache**,**费用降 90%**;
- **之下**（环境信息/CLAUDE.md/记忆/MCP）因人而异，实时生成。

由此构成**三级缓存**：全局缓存（跨组织跨用户）→ 组织缓存（同组织跨会话）→ 会话缓存（同 Section 只算一次）。一句话:**用最小的 token 成本，给模型划最清晰的行为边界**。

---

## 六、上下文管理与 Compact 压缩

### Q19：为什么 Agent 分分钟爆上下文窗口？加大窗口不行吗？

三处叠加：开局固定开支 5k~10k(system prompt+工具描述+CLAUDE.md);**工具调用留 tool_use + tool_result 两条记录且每轮反复计费**；大文件 Read 动辄几千上万 token。

扩窗口治标不治本（三个硬伤）:**钱**(token 几何级涨）、**慢**(Attention 平方复杂度,TTFT 变高）、**Lost in the Middle**（注意力固有特性，窗口再大中间段照样看不清——所以 1 亿 token 时代仍需要 compaction)。

### Q20：常见的上下文管理方案为什么不够看？

- **滑动窗口**:「用遗忘换续航」——开局的全局指令被砍就失控,tool_result 被砍后续引用成无源之水；
- **每 N 轮摘要**：触发时机死板、粒度太粗；
- **向量召回**:①Agent 上下文**强时序依赖**，召回不管顺序；②tool_use/tool_result 可能被切散；③top-k 必然漏掉相似度低但关键的消息。RAG 检索文档行，管 Agent 上下文不行。

### Q21:Claude Code 的上下文压缩机制？（面试最爱，四层答法)

**开场**:「不是滑动窗口/定期摘要/向量召回，而是**全量重写 + 分通道恢复**。」

**前置**:5 层金字塔，能轻则轻、每层为下一层减负：
| 层 | 手段 | 触发 |
|---|---|---|
| 1 | 大结果存磁盘（>50KB 存盘留 2KB 预览，单消息总量 ≤200KB) | 零 API 开销 |
| 2 | Snip 砍远古消息（模型正常回合顺手用 snip 工具标记，释放量传给第 5 层） | 很轻 |
| 3 | Micro-Compact 时间衰减（清可重获取工具结果留最近 5 个；子 Agent/Task 结果绝不裁） | 距上次 API 调用 >60 分钟（Prompt Cache 过期） |
| 4 | Context Collapse 读时投影（不改原始消息，调 API 时算压缩视图） | 90% 触发/95% 升级；**与第 5 层互斥二选一**,Anthropic 内部灰度特性 |
| 5 | Auto-Compact 全量摘要 | 距窗口上限约 33k |

**第一层·触发时机**：绝对 token 阈值——「有效窗口 - 13k」。细节之王：有效窗口已先扣掉 **20k 摘要输出预留**（依据 p99.99 实测 17,387 token 向上取整），再加 13k 独立安全线，**离原始窗口实际 ≈33k**。绝对值比 80% 比例更可预测（窗口膨胀时预留不跟着膨胀）。手动 /compact 可传 customInstructions；自动模式开 `suppressFollowUpQuestions`（不打断干活节奏）。安全机制:**连续失败 3 次熔断**（曾有 1000+ 会话压缩失败反复重试烧账单）+ **querySource 递归守卫**（摘要子任务标记 compact 不再触发压缩）。

**第二层·取舍逻辑**:**不保留最近 N 条，全部一刀切送摘要器重写**(Lost in the Middle 让「留最近 20 轮」也没意义）。压缩后四段式：边界标记 + 摘要 + 附件 + hook 结果。信息分通道:**语义信息走摘要，状态信息走附件**（最近 5 个文件、每个 ≤5k、总预算 ≤50k；异步任务状态；计划文件）;**CLAUDE.md 不进摘要**——清空 getUserContext 缓存让下轮自动重载；system prompt 压缩后重建（顺便刷新工具列表）。先跑 microcompact 预处理（清掉大工具结果）再送摘要器。

**第三层·摘要 prompt 设计**:200+ 行，开头结尾**两遍「禁止工具调用」**(Sonnet 4.6 时代留下的坑，模型手痒调工具摘要就废了）;XML 格式（`<analysis>` 草稿区剥离 + `<summary>` 正式区）;**9 部分清单**，两个灵魂项:⑥**所有用户消息必须枚举不能落**（需求变更信号丢了就走偏）;⑧**当前工作精确到文件和函数名**。摘要用**当前对话同一模型**——保质量 + 复用 Prompt Cache。

**第四层·接续机制**：清缓存 → 并发生成附件 → buildPostCompactMessages 组装新链替换旧链；摘要开头包一句「**本会话是从上下文耗尽中断的对话延续过来的**」让模型知道是接力；末尾带 transcript 路径留「翻底牌」通道。旧消息真丢（除非 Kairos 模式备份）,**一刀切反而干净**。

**收口**:「这不是省 token 的小聪明，而是**信息分通道管理**的工程哲学——不同信息有不同半衰期，要分别管理。」

---

## 七、记忆机制

### Q22：你的 agent 项目记忆机制怎么做？（60 分 vs 95 分)

60 分:「上向量数据库做 embedding 相似度检索」。95 分四步走：

**第一步**:LLM 无状态，记忆机制 = 「给失忆实习生贴便签」，核心问题是贴在哪、谁来贴、什么时候撕。

**第二步**：业界四类方案（滑动窗口/对话摘要/向量检索/分层存储）的共同病根:**自由文本无约束、不区分类型、没有老化机制、重检索轻写入**。

**第三步**:Claude Code 反例——没用一行 embedding，用「**结构化 markdown 文件 + LLM 选择**」:
- **两层架构**：静态层 CLAUDE.md 六层级（Managed/User/Project/Local/Auto/Team，声明式）+ 动态层自动记忆系统（学习式）;
- **四种类型封闭集合**:user（画像）/feedback（偏好，强制 规则+Why+How to apply 三段）/project（动态，相对日期转绝对日期）/reference（外部指针）——逼 agent 写入前先做分类决策；
- **排除清单**：代码可推导的一律不存（架构/git 历史/调试方法/CLAUDE.md 已有内容）——「代码是活的，记忆是死的」,过时记忆是「权威的错误」;
- **存储**:MEMORY.md 索引常驻 system prompt(**200 行 + 25KB 双保险**，防实测 197KB 的「长行索引炸弹」)，独立 md 文件按需加载——「只塞目录，不塞正文」;
- **写入**:extractMemories 后台代理，每轮结束 stopHook 触发，**fork 主对话复用 prompt cache**，跟现有记忆比对去重；
- **检索**:Sonnet 当选择器（只读每个文件前 30 行 frontmatter → 拼清单选 top-5 → 宁少选不错选）,alreadySurfaced/recentTools 过滤；用 Sonnet 不用 Haiku 因为判错会污染整个回复；
- **注入**:system-reminder 包裹 + **2 天老化警告** + 用前先验证（文件路径先查存在、函数名先 grep)。

**第四步**：四条可迁移原则——**结构化优于自由文本；索引常驻 + 内容按需；廉价模型做选择题（候选几百以内 > 向量检索）；时间感知 + 主动验证**。

金句:**「记忆不是真理，是历史快照。」**

---

## 八、代码检索:grep vs RAG

### Q23：为什么 Claude Code 不用 RAG 检索代码，而是直接用 grep?（字节真题)

**三层答案**:

**第一层·RAG 在代码场景的五大痛点**:①代码切不动（函数拦腰切，一段少 if 一段少 else，跨片段调用关系断裂）;②向量找「相似」不是找「对」（要 getUserById，回来 getUserByName 一堆）;③代码每天在变，索引重建贵、不建则滞后、增量更新一堆边界情况;④冷启动慢（百万行建索引十几分钟，违背「打开就能用」);⑤黑盒不可解释（Top-K 为什么是这 5 个没人答得上）。代码是**动态、结构化、需要精确**的，恰是 RAG 主场的反面。

**第二层·Claude Code 的方案 = 把检索决策权还给 LLM**：程序员怎么找代码它就怎么找——**Glob（按文件名，按修改时间排序，100 文件上限）/ Grep(ripgrep 底层、三种输出粒度、权限管控）/ Read（默认 2000 行、offset/limit 分段、实时读磁盘）**。有 Bash 还要 Grep 工具的原因：权限闸门、结构化输出省 token、性能。复杂探索派 **Explore 子 agent**（只读工具池、上下文隔离、只回结论）,**临界点是「预期超过 3 次查询」**，可并行派多个。

**第三层·设计哲学**:RAG 派「LLM 不够强，工程替它做决定」;Claude Code 派「LLM 已足够强，**工程的角色是备好工具、把决策权还给它**」——押注模型越来越强。加上 while 循环的**多轮迭代**:RAG 是「考试发卷子」一锤子买卖，召回错将错就错;Agent 是「现场探案」，每步根据上一步结果调整方向。

**防反问**:RAG 仍有舞台——巨型跨仓库检索、纯语义模糊查询、代码+文档+Wiki 混合知识库。**工具为场景服务，没有银弹**。

---

## 九、多 Agent(SubAgent)

### Q24:Claude Code 的多 Agent 是怎么做的？

**三套机制**:常规 Subagent（父子型）、**Fork Subagent**（缓存优化的父子分身）、**Coordinator 模式**（主从型）。

**为什么要多 agent**：单 agent 扛复合任务有三麻烦——上下文爆炸、职责混乱、没法并发。

**隔离（最关键）**:
- **工具隔离三道门**:①全局黑名单（禁派子 agent/禁问用户/禁切规划模式/禁停任务——防递归、防抢对话权）;②自定义 agent 加严;③后台异步 agent 走白名单（默认不准，列出才能用）;
- **上下文隔离按字段粒度**（不是全共享也不是全新建）:①读文件缓存**克隆**（防子污染父的文件视图）;②写全局状态**关闭**（防抢 UI 状态）;③注册后台任务通路**例外保留**（防孤儿进程）;④独立 ID + **深度 +1**（嵌套超阈值报警）。

### Q25：父子 Agent 之间怎么通信？（讲清边界最显功底)

分水岭是**开没开 agent-teams 模式**:
- **默认形态**:subagent ≈ 一次「重型工具调用」，父派出去只能等，**子→父单向**。两个补丁:**auto-background**(>2 分钟自动转后台，同步降级为异步）+ **完成通知 = task-notification XML 伪装成用户消息**塞进父的对话历史（XML 对 LLM 友好、纯文本可入历史、天然复用 agentic loop——「把系统事件伪装成对话」);
- **团队模式**：补齐父→子——**SendMessage 工具往子的 pendingMessages 信箱扔字条**（扔完就走），子每轮循环边界自己捡；子已完成会从磁盘 transcript **唤醒**重跑。落到底就两个关键字：**异步 + 消息**。

### Q26:Fork Subagent 是什么？（成本优化亮点)

背景:system prompt 上万 token;**Prompt Cache 命中要求字节级完全相同**，命中后价格只要 10%。
做法：派一个与父 agent 前缀**字节级相同**的分身复用缓存。`CacheSafeParams` 五项必须对齐:system prompt、userContext、systemContext、**工具池顺序和定义**、对话历史前缀。精妙细节：Fork 的 `getSystemPrompt` **直接返回空串**——直接用父已渲染好的字节，重新生成可能差一个字符就丢缓存。
边界：适合「需要父完整上下文但不想污染父主循环」（生成 PR 描述等）；专业分工定制 prompt 的用常规 subagent;**与 Coordinator 互斥**。
启示:**成本优化本身就是能力的一部分**——成本降 90%,subagent 就敢派得更频繁。

### Q27:Coordinator 模式是什么？

开启:feature 开关 + `CLAUDE_CODE_COORDINATOR_MODE=1`。主 agent 从全能选手退化为**纯协调者**，只做三件事:**派 worker、收结果、合成答案**。

- **协调者工具箱**（派 worker/创建解散团队/SendMessage/合成输出/停止 worker）从 worker 工具池摘掉——worker 不能派 worker，**扁平两层防递归**;
- **并行是超能力**:同一条 assistant 消息可含多个派 worker 调用，底层并发执行（串行等 10 分钟 vs 并行 3 分钟）;
- **流水线四阶段**：调研（workers 并行）→ **合成（协调者亲做）** → 实现（workers)→ 验证（workers);
- **核心哲学：协调者必须「理解」不能「转发」**——不做「based on your findings, implement」的传话筒;
- **Continue vs Spawn**：上下文相关续命老 worker（省钱）；不相关或走偏派新的；**验证任务永远派新 worker**（新鲜眼光，不能自己验自己）。

**五条 Multi-Agent 设计原则**:①上下文隔离按字段粒度做；②通信走消息不走函数调用；③工具权限分级管控；④缓存友好是架构能力；⑤并行优先 + 协调者合成（扁平不递归）。

---

## 十、Skill 机制

### Q28:Skill 和提示词有什么区别？为什么不直接塞进 system prompt?（高频 + 追问)

**速答**:「Skill 的内容确实是提示词，但区别不在内容，在**加载方式**。提示词一股脑全塞上下文;skill 走**渐进式披露（Progressive Disclosure)**——平时只常驻一行描述（占窗口 1%)，模型判断用得上才注入正文，参考文档再懒一层真用到才读。**提示词是知识本身，skill 是知识的加载策略。**」

展开:
- **算笔账**:50 个 skill × 3000 token = 15 万 token,20 万窗口占 3/4；渐进式披露压到 2000 token(75% → 1%);
- **三层**:①书脊清单（name+description，单条 250 字符上限，超预算降级：等比截短 → 第三方只留名字，**官方内置永不截断**);②选中才注入完整 SKILL.md——**包装成 isMeta 用户消息**而非改 system prompt（不伤 Prompt Cache);③references/scripts 靠 SKILL.md 里指路，模型真需要才自己 Read;
- **选得准**:embedding/意图分类/关键词**全没用**——清单摆在那，**靠模型自己的阅读理解**;description 是唯一广告位，配 BLOCKING REQUIREMENT 硬指令防「想起来懒得用」;
- **斜杠命令 = skill**：源码里同一套 Command 体系。两个入口（人敲 / 模型自主）× 两个开关（`disable-model-invocation` 危险操作只留人扣扳机 / `user-invocable:false` 纯背景知识只对模型可见）;
- **动态内容**:`$ARGUMENTS` 参数插值 + `` !`命令` `` 预执行（如 /commit 内嵌 git diff);**MCP 远程 skill 禁止执行内嵌命令**（不可信）;
- **五个来源**：内置/用户级/项目级（**随 git 走，团队知识变活文档**)/插件/MCP；撞名**先到先得**，项目级排在用户级后加载。

**写 skill 三心法**:description 按广告词写「什么时候用我」;SKILL.md 超 500 行拆 references;**能固化成脚本的别让模型现想**。

---

## 十一、60 秒电梯版 & 数字速查表

### 60 秒电梯版（被问「你了解 Claude Code 吗?」）

> 「Claude Code 是 Harness Engineering 的教科书——80% 代码在死磕可靠性而非模型智商。架构上四层分离，引擎层不含业务逻辑；Agent 循环不用 ReAct，是一个极简 Tool-Use Loop，信任模型用 Extended Thinking 内部推理，API 原生 tool_use/end_turn 驱动。System Prompt 动态组装 + 分割线共享 Prompt Cache 省 90% 费用。上下文管理是 5 层从轻到重的压缩金字塔，兜底的全量摘要是『全量重写 + 分通道恢复』。记忆不用向量库，用四类型 markdown + 索引常驻 + Sonnet 选择器 + 老化警告。代码检索不用 RAG，用 Grep/Glob/Read 三件套把决策权还给模型，复杂探索派 Explore 子 agent 隔离上下文。多 Agent 有三套机制，Fork 复用缓存降本 90%,Coordinator 让主 agent 退化成纯协调者最大化并行。Skill 是渐进式披露的懒加载，常驻开销只有窗口 1%。贯穿始终的哲学就一句：**信任模型能力，工程负责划边界、管好上下文和成本。**」

### 数字速查表（合并版)

| 数字 | 含义 |
|---|---|
| 92% / 70% / 96% | CLAUDE.md 200 行 / 400 行 / 模块化 rules 的规则遵守率 |
| 200 行 / 80 行 | CLAUDE.md 黄金线 / 推荐起步行数 |
| 40+ | 工具数量 |
| 17 种 | 主循环退出原因数 |
| 1729 行 | query.ts 规模（80% 是异常路径） |
| 8k→64k / 3 次 | 输出截断静默升档 / 续写上限 |
| 90% | Prompt Cache 命中省的费用；Context Collapse 触发阈值 |
| 17,387 / 20k / 13k / ≈33k | 摘要输出 p99.99 / 输出预留 / 安全线 / Auto-Compact 离窗口总缓冲 |
| 50KB / 2KB / 200KB | 单工具结果存磁盘阈值 / 预览 / 单消息总量上限 |
| 60 分钟 | Micro-Compact 触发（Prompt Cache 过期） |
| 5 个 / 5K / 50K | 压缩后恢复文件数 / 单文件 / 总预算 |
| 9 部分 | 摘要 prompt 清单（⑥枚举用户消息 ⑧当前工作精确到函数名） |
| 3 次 | 压缩熔断 / 摘要失败熔断 |
| 4 类 / 6 层 | 记忆类型(user/feedback/project/reference)/ CLAUDE.md 层级 |
| 200 行 / 25KB | MEMORY.md 索引双上限 |
| 前 30 行 / top-5 / 2 天 | 记忆召回扫描范围 / Sonnet 召回条数 / 老化警告阈值 |
| 2000 行 / 100 个 / 3 次 | Read 默认上限 / Glob 上限 / 直接检索 vs 派 Explore 子 agent 临界点 |
| 2 分钟 | subagent auto-background 阈值 |
| 10%~90% | Prompt Cache 命中价格 / Fork 省的成本 |
| 1% / 250 字符 / 500 行 | Skill 清单窗口预算 / 描述上限 / SKILL.md 拆分线 |

---

## 贯穿全文的一条主线（终极升华)

无论 CLAUDE.md、主循环、压缩、记忆、检索、多 Agent 还是 Skill,Anthropic 的所有选择都指向同一个哲学:

> **信任模型，不替模型做决定；工程的价值在于划好边界（安全/权限)、管好上下文（压缩/记忆/渐进加载)、控制好成本（缓存/Fork)。能靠模型自身理解力解决的，绝不外挂一套额外系统——简单组件的组合，胜过花哨的黑科技。**
