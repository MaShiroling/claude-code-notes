# Claude Code 记忆机制面试手册

> 整理自《Claude Code 记忆机制图解：为什么不用向量数据库？》(公众号 @小林 coding)
> 面试高频题:「你这个 agent 的记忆机制是怎么做的?」——「上向量数据库做 embedding 检索」是 60 分答案，本文给你 95 分答法。

---

## 〇、一句话核心

LLM 是**无状态**的（金鱼记忆），记忆机制的本质是「在工位上贴便签」——核心问题是**贴在哪、谁来贴、什么时候撕**。Claude Code 没用一行 embedding、没用一个向量数据库，全靠**结构化 markdown 文件 + 小模型当选择器**，反而比向量检索好用。

---

## 一、先搞懂:LLM 其实没记忆

- LLM 彻底无状态：每次请求都是「从头看一遍」(system prompt + 历史 + 当前问题）；你以为它记得，其实是客户端偷偷把历史一起发过去。
- 聊天场景凑合能用，agent 不行——agent 在长跑，上下文会爆窗口；而且 agent 要记的不是「历史对话」而是**事实与规则**（用户画像、行为偏好、项目动态、外部指针）。
- **短期记忆 vs 长期记忆**（坐标轴）:
  - 短期 = 上下文窗口本身，满了靠 compaction 压缩；
  - 长期 = 持久化到磁盘、跨会话存活。本篇拆的是**长期记忆**。
- 类比：失忆症实习生——很聪明但每天从零开始，记忆机制就是管「便签贴在哪、谁来贴、什么时候撕」。

---

## 二、业界四类主流方案及其共同病根（面试必背）

| 方案 | 代表 | 硬伤 |
|---|---|---|
| 滑动窗口 Memory | LangChain buffer window（已弃用） | 关键信息和无关信息一起被丢 |
| 对话摘要 Memory | LangChain summary memory | 重要细节被压糊；摘要本身耗 token、有延迟 |
| 向量检索 Memory | Mem0(LoCoMo 91.6 分）、Letta、Zep | ①相似≠相关，噪音淹没；②召回不稳定（换 embedding 模型就变）；③维护成本高（向量库/embedding 选型/chunk/索引更新）;④**用户没法看**(768 维浮点数不可读、难 debug) |
| 分层存储 Memory | MemGPT/Letta(core/recall/archival) | 概念多、迁移成本高；底层还是 embedding 召回，前面硬伤一个没躲过 |

**四个共同病根**:

1. **自由文本无约束** → 记忆库膨胀成垃圾堆；
2. **不区分类型** → 画像/动态/指针一锅炖，哪个都查不准；
3. **没有老化机制** → 过时记忆成「权威的错误」，比没记忆还糟；
4. **重检索、轻写入** → 「该不该存、存什么」放任不管，垃圾进垃圾出。

---

## 三、Claude Code 两层记忆架构（鸟瞰)

- **静态层 = CLAUDE.md 体系**：声明式指令，启动时全量加载，管「我们怎么协作」这种确定性的事（员工手册）;
- **动态层 = 自动记忆系统**：学习式偏好，agent 互动中自动写记忆文件，按需检索，管「我从互动中学到了什么」（自己的工作笔记）。

---

## 四、静态层:CLAUDE.md 六层级

为什么不能只有一个 CLAUDE.md？因为规则来源不同、可见范围不同、谁能改不同。按加载顺序从低到高：

| 层级 | 位置 | 用途 |
|---|---|---|
| Managed | 系统级路径 | 公司级强制策略，只有管理员能改 |
| User | 用户家目录 | 个人全局偏好，跨项目生效 |
| Project | 项目根 `CLAUDE.md` / `.claude/CLAUDE.md` | 项目规则，签入 git 团队共享 |
| Local | `CLAUDE.local.md` | 默认不签 git，本地自用 |
| Auto(AutoMem) | 项目级自动记忆目录 | Claude 自动写入的偏好 |
| Team(TeamMem) | Auto 下 `team/` 子目录 | 团队共享的 AI 学到的偏好（feature flag 开启） |

六层是**叠加不是覆盖**，全部拼进 system prompt。（后两层存的是 MEMORY.md，同属一套加载体系。)

**三个配套机制**:

1. **`@include` 引用**:`@~/company/security-rules.md` 自动读入拼接（类比 C 的 #include)，背后防循环引用、防路径遍历；
2. **条件规则（path-scoped)**:`.claude/rules/` 下每个 md 的 frontmatter 写 `paths: ["**/*.tsx"]`，只在编辑匹配文件时注入——「按需注入」而非「一次全塞」;
3. **截断双保险**:MEMORY.md 索引同时限 **200 行 + 25KB**。缘由：实测 p100 极端情况是不到 200 行却有 197KB（长行索引炸弹），光看行数察觉不到。截断时追加警告告知模型。**凡用户可控文本要进 system prompt，都建议这种双保险**。

---

## 五、动态层：自动记忆系统的完整闭环（重头戏）

### 1. 记什么：四种类型（封闭集合）

```typescript
export const MEMORY_TYPES = ['user', 'feedback', 'project', 'reference'] as const
```

- **user**：用户画像（你是谁、擅长什么）→ 回答因人而异；
- **feedback**：行为偏好（最重要），**强制三段结构**：规则本身 + **Why**（为什么，往往是踩过的坑）+ **How to apply**（什么情况生效）——没 Why 遇到边界情况就抓瞎；
- **project**：项目动态，同吃 Why/How 结构（最易过期，需让 agent 判断「现在还该不该信」);**相对日期必须转绝对日期**(「周四前冻结」→「2026-03-05 前冻结」);
- **reference**：外部指针（去哪查什么），不需要知道内容，只需知道去哪找。

限定四类的本质：**逼 agent 写入前先做分类决策**，不一股脑乱塞。

### 2. 不记什么：排除清单（纪律）

代码模式/架构/文件路径、Git 历史、调试修复方法、CLAUDE.md 已有内容、临时任务状态——一律不存。
核心原则：**代码是活的，记忆是死的；只记代码推不出来的东西。** 过时记忆 = 「权威的错误」，比没有更糟。

### 3. 怎么存：单文件 + 索引（解决两难）

两难：全塞 system prompt 会爆（50 条×200 字≈几千 token)；完全不塞 agent 不知道有什么。
答案：**只塞目录，不塞正文**（像工具书：背目录，需要时翻页）。

- 每条记忆 = 独立 `.md` 文件 + YAML frontmatter(name / description / type;description 决定能否被检索到）;
- `MEMORY.md` 索引**始终加载进 system prompt**；独立文件**按需加载**。

### 4. 怎么写：Extract Memories 后台代理

为什么不让主对话自己写？①模型分心，回复质量打折；②每轮 system prompt 塞「记偏好」指令浪费 token。

方案：每轮 query loop 完整结束后，通过 **stopHook** 触发 `extractMemories` 后台代理：

- **fork 主对话复用 prompt cache**(`runForkedAgent`)——不重载几千 token 的 system prompt，多花的钱很少；
- 逻辑：扫本轮用户反馈/纠正 → 跟现有记忆比对去重（`hasMemoryWritesSince` 防反复写）→ 有新内容才按四类写新文件。
- 这正是在治「重检索、轻写入」的病根。

### 5. 怎么检索：Sonnet 选 top-5（不用向量！)

`findRelevantMemories` 三步：
1. 扫描所有记忆文件**只读前 30 行**(frontmatter),200 个文件开销也很小；
2. 拼成标题清单 + 用户当前 query 发给 **Sonnet**，要求 JSON schema 返回 top-5 文件名；系统提示极严苛：「不确定的就别选」——**宁可少选，不可错选**;
3. 加载选中记忆的完整内容注入。

- **为什么 Sonnet 不用更便宜的 Haiku?** 记忆判错会污染整个回复，容错率极低，宁贵要准。
- **alreadySurfaced 过滤**：上轮已露脸的记忆排除，名额花在新候选上；
- **recentTools 过滤**：正在用的工具的「用法文档」是噪音不选，但「警告/坑点」要保留——**检索不只是匹配，还要看当前上下文已有什么**。

### 6. 怎么注入：system-reminder 包裹 + 老化警告

- 用 `<system-reminder>` 标签包裹注入（防止被当成用户刚说的话）;
- **时间感知**:≤1 天不警告；≥2 天自动附加「This memory was saved N days ago. Verify it's still accurate before acting on it.」;
- **主动验证**(「Before recommending from memory」)：记忆里的文件路径先检查存在、函数名/flag 先 grep、用户要照做前先验证。
- 心法：**记忆不是真理，是历史快照**；对待记忆应像对待 git log——「这是过去发生的事」，不是「这是现在的状态」。

---

## 六、四条可迁移的设计原则（抄作业清单）

1. **结构化优于自由文本**：给记忆定 schema 强制填齐，哪怕只有 4 个字段；自由文本 = 三个月后的垃圾堆；
2. **索引常驻 + 内容按需**:MEMORY.md 常驻 system prompt，正文按需加载——适用于任何「总量大但只需展开少数」的场景（长 RAG 文档、知识库、tool 列表、历史 PR);
3. **廉价模型做选择题**：候选集合几百以内时，用小模型选择 > 向量检索——自然语言判断可解释、可调，比调相似度阈值容易得多，成本比维护向量库便宜；
4. **时间感知 + 主动验证**：给「权威的错误」上锁——过时记忆加 stale 警告，使用前先 grep/检查文件验证。

---

## 七、面试速答模板（95 分版，四步）

**第一步**：指出 LLM 是无状态的，记忆机制本质是「在工位上贴便签」，核心问题是贴在哪、谁来贴、什么时候撕。

**第二步**：指出业界主流方案的共同病根——滑动窗口、对话摘要、向量检索、分层存储四类，共同问题是自由文本无约束、不区分类型、没有老化机制、重检索轻写入。

**第三步**：举 Claude Code 反例——没用向量数据库，用「结构化文件 + LLM 选择」:
- 两层架构：CLAUDE.md 六层级（声明式）+ 自动记忆系统（学习式）;
- 四种类型：user/feedback/project/reference，强制分类决策；
- 索引常驻：MEMORY.md 始终加载、内容按需加载；
- 小模型选择：Sonnet 选 top-5，过滤已露脸 + 过滤工具文档；
- 老化警告：2 天前的记忆主动加 stale 提醒，逼模型主动验证。

**第四步**：给出可迁移原则——结构化优于自由文本、索引常驻+内容按需、廉价模型做选择题、时间感知+主动验证。

---

## 八、收尾金句

> 这套设计的精髓与 Claude Code 整体哲学一致：**不堆复杂度，把成熟的简单组件（文件系统 + LLM）组合出比花哨方案更好用的东西。**

---

## 关键数字速查

| 数字 | 含义 |
|---|---|
| 4 种 | 记忆类型 user/feedback/project/reference |
| 6 层 | CLAUDE.md 层级 Managed/User/Project/Local/Auto/Team |
| 200 行 / 25KB | MEMORY.md 索引双上限 |
| 197KB | 实测极端：不到 200 行的索引炸弹（双保险的由来） |
| 前 30 行 | 检索时每个记忆文件只读的 frontmatter 范围 |
| top-5 | Sonnet 每次召回的记忆条数 |
| 2 天 | 记忆老化警告阈值 |
| 91.6 分 | Mem0 在 LoCoMo 基准的成绩（向量派最强选手） |
