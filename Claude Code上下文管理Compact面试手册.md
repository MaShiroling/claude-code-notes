# Claude Code 上下文管理(Compact)面试手册

> 整理自《Claude Code 上下文管理图解：Compact 压缩机制怎么实现？》(公众号 @小林 coding)
> 面试高频题:「你这个 agent 项目的上下文是怎么管的?」

---

## 〇、一句话观点（开场就亮）

> 「Claude Code 用的不是滑动窗口、定期摘要、向量召回那一类常见方案，而是一种叫**全量重写 + 分通道恢复**的工程化思路。」

收尾金句：**上下文管理不是「省 token」，是「保信息结构」——不同信息有不同的半衰期，要分通道管理。**

---

## 一、上下文窗口基础 & 为什么 Agent 分分钟爆窗口

- 模型没有真正的记忆，每次请求都是「从头看一遍」:system prompt + 历史对话 + 当前问题，总长度上限即上下文窗口（单位 token)。
- 主流窗口：GPT-4 早期 8k、Claude 3.5 Sonnet 200k、Claude Opus 1M 版、部分 Gemini 2M。
- **Agent 爆窗口的三处叠加**:
  1. **开局大头**:system prompt + 工具描述 + CLAUDE.md 固定开支 5k~10k;
  2. **工具调用留两条记录且持续累积**:tool_use + tool_result 成对出现,tool_result 常含几千 token 文件内容，**之后每一轮都被反复加载计费**;
  3. **大文件 Read 杀伤力巨大**：一个源文件几千到一万 token，读三五个就堆满。
- 算账：开局 8k + 5 个 5k 文件 = 33k，跑十几轮几万 token，长任务分分钟逼近 200k。

### 扩窗口为什么治标不治本（三个硬伤）

1. **钱**:token 消耗随上下文几何级上涨；
2. **慢**:Attention 复杂度与序列长度平方相关,TTFT（首 token 延迟）随之变高——5k 对话 1 秒返回，150k 可能要十几秒；
3. **Lost in the Middle（最致命）**：模型对首尾信息记得清、中间段模糊，是注意力机制固有特性，**窗口扩到 1 亿 token 也照样存在**——所以主动管理信息结构永远不会过时。

---

## 二、业界常见方案为什么不够看

| 方案 | 做法 | 硬伤 |
|---|---|---|
| 滑动窗口 | 超阈值从最老的消息开始砍 | 「用遗忘换续航」：开局的全局指令被砍 → Agent 失控;tool_result 被砍 → 后续引用变无源之水 |
| 每 N 轮摘要 | 定期把小模型总结替换原文 | 触发时机死板（关键节点被一刀切）；粒度太粗，状态/错误修复/需求变更全丢 |
| 向量召回 | 历史切片入向量库，按相似度召回 top-k | ①Agent 上下文**强时序依赖**，召回不管顺序；②tool_use/tool_result 可能被切散；③top-k 必然漏掉相似度低但关键的消息 |

结论：RAG 检索文档行，管 Agent 上下文不行——两个完全不同的场景。

---

## 三、Claude Code 的 5 层压缩金字塔

**设计原则：能不压就不压，必须压就从最轻的手段开始。**（每层为下一层减负，绝大多数场景走不到顶层）

| 层 | 手段 | 信息损失 | API 开销 | 触发条件 |
|---|---|---|---|---|
| 1 | 大结果存磁盘 | 几乎为零 | 零 | 单工具结果 > 50KB，留 2KB 预览；单消息工具结果总量 ≤200KB |
| 2 | Snip 砍远古消息 | 低 | 很轻 | 模型在正常回合里顺手用 snip 工具按消息 id 标记，本地删除 + 边界标记；释放量传给第 5 层防重复压缩 |
| 3 | Micro-Compact 时间衰减 | 中低 | 零 | 距上次 API 调用 >60 分钟（Prompt Cache 已过期）；清空「可重新获取」工具结果只留最近 5 个，子 Agent/Task 结果绝不裁 |
| 4 | Context Collapse 读时投影 | 中 | 低 | 上下文达 90% 触发、95% 升级；**不改原始消息，调 API 那一刻动态计算压缩视图** |
| 5 | Auto-Compact 全量摘要 | 高 | 高（一次 API 调用） | 距窗口上限约 33K 缓冲 |

**关键澄清（面试加分细节）**:
- 第 4 层和第 5 层**互斥二选一**，有开关控制；Context Collapse 启用时 Auto-Compact 的触发判断被短路。原因：触发线咬太近（90%/95% vs 93%)，同跑会打架。
- Context Collapse 目前是 Anthropic **内部灰度实验特性**，公开版本代码被裁掉——翻公开源码大概率只能看到 Auto-Compact 这条线。
- 第 2 层 Snip 不是字面意义的「零 API 纯本地」，是搭着模型正常回合顺手标记，顶多多注入一小段提示 + 消息 id 标签。
- 第 3 层 microcompact 有自己独立的生命周期（按 60 分钟时间衰减单独触发），同时在 Auto-Compact 流程里扮演「预处理」角色。

---

## 四、Auto-Compact 三大核心设计

1. **绝对阈值触发**：不按轮数、不按百分比，按「距窗口上限的固定 token 缓冲」;
2. **全量重写对话**：所有历史消息不分新旧，全部送摘要器重写一份（最反直觉）;
3. **关键信息走另外的恢复通道**：文件内容、CLAUDE.md、异步任务状态不靠摘要，靠「重新注入」。

类比：老板做季度总结——会议记录归档（丢了）、桌面换上精华版纪要（摘要）、员工手册原地不动下轮重新加载（CLAUDE.md)、最近用的关键文档放随手翻的位置（附件注入）。

---

## 五、触发时机：阈值怎么算（面试细节之王）

```typescript
// 基于摘要输出 p99.99 = 17,387 token 的实测统计
const MAX_OUTPUT_TOKENS_FOR_SUMMARY = 20_000
export const AUTOCOMPACT_BUFFER_TOKENS = 13_000
export function getAutoCompactThreshold(model) {
  return getEffectiveContextWindowSize(model) - AUTOCOMPACT_BUFFER_TOKENS
}
```

**两层缓冲别搞混**:
- **第一层 20k**：先从原始窗口里抠掉，留给摘要自己的输出。依据是 Anthropic 真跑了大规模统计——摘要输出 p99.99 分位仅 17,387 token，向上取整加冗余凑整 20k;
- **第二层 13k**：在「原始窗口 - 20k」的有效窗口之上再缩的独立安全线，让压缩提前一点触发、别贴边界跑。
- 所以真正离原始窗口的距离是 **20k + 13k ≈ 33k**，不是单看到的 13k。

**为什么用绝对值不用 80% 比例？** 窗口扩到 500k/1M 时触发线永远是「上限 - 33k」不会跟着膨胀；比例制下窗口越大剩余预算越大，但摘要实际需要没变——纯浪费。绝对阈值**可预测**。

### 手动 /compact vs 自动触发

同一个核心压缩函数，参数不同：
- **手动**：可传 `customInstructions`（如「重点保留这个 bug 的上下文」);
- **自动**：打开 `suppressFollowUpQuestions` 开关——摘要里禁止生成「需要进一步确认」类问题（自动压缩发生在干活途中，不想被打断节奏）。

### 两个安全机制

- **Circuit Breaker 熔断**：auto-compact 连续失败 3 次停止重试。踩坑由来：曾有 1000+ 会话因压缩失败反复重试把 API 账单当烟花放。
- **递归守卫**：摘要任务本身也是子 agent 调模型，靠 `querySource` 标签防无限递归:

```typescript
if (querySource === 'session_memory' || querySource === 'compact') return false
```

---

## 六、压什么、留什么、丢什么（全量重写）

**反直觉点：不保留最近 N 条，200 轮全部一刀切送摘要器重写。** 理由：就算留最近 20 轮,Lost in the Middle 也让模型看不清中间——不如全部压成结构化精华。最近的信息走另外的恢复通道。

### 压缩后的四段式消息结构

```typescript
return [
  result.boundaryMarker,     // 1. 边界标记：自动/手动、压缩前 token 数、最后消息 ID
  ...result.summaryMessages, // 2. 摘要消息(大头)
  ...result.attachments,     // 3. 附件：最近文件、计划文件、激活技能、异步任务状态
  ...result.hookResults,     // 4. hook 执行结果
]
```

### 信息分通道管理（本节灵魂）

> **不同信息走不同通道，谁也别碰谁的地盘。**

- **语义信息 → 走摘要**:「用户想给登录接口加验证码」「方案改成了 JWT」压成一两句话基本无损；
- **状态信息 → 走附件**:「a.py 第 42 行的 bug 在改」「子任务 X 输出在 result.txt」差一个字就接不上，必须原样恢复。

各通道细节：

1. **microcompact 预处理**：全量摘要前先清空大工具结果（Read/Bash/Grep/Glob/WebFetch/WebSearch/Edit/Write）只留元数据占位符 → 对话先瘦一圈，摘要器负担小、质量高；
2. **文件恢复「5 个、5K、50K」**：压缩后按最近活跃度最多重载 5 个文件、每个 ≤5k token、总预算 ≤50k token——把「最近文件」量化成可控参数;
3. **CLAUDE.md 不进摘要**（反直觉）：压缩时清空 `getUserContext` 缓存，下一轮自动从磁盘重新加载。永久指令和当下对话是两套机制，互不干扰;
4. **system prompt 不参与压缩**：压缩后用 `buildEffectiveSystemPrompt` 重建，注入最新工具列表/权限/MCP server——中途加的新工具立刻可用;
5. **异步任务状态保活**：后台子 agent 的运行状态作为附件重新注入，主 agent 压缩后仍知道子任务进展。

---

## 七、摘要 Prompt 设计（两百多行）

### 防呆设计：禁止工具调用（前后包夹喊两遍）

```
CRITICAL: Respond with TEXT ONLY. Do NOT call any tools.
- Do NOT use Read, Bash, Grep, Glob, Edit, Write, or ANY other tool.
- Tool calls will be REJECTED and will waste your only turn.
```

原因：摘要的本质是只生成文字，但模型看到「这个文件之前 Read 过」容易手痒去调工具，一调这次摘要就废了。源码注释：早期 Sonnet 4.6 经常无视一次警告，于是开头结尾各喊一遍。

### 输出格式:XML + 9 部分清单

```xml
<analysis>[推理草稿区，最终会被剥离，不进压缩后的对话]</analysis>
<summary>[结构化摘要，按 9 个清单分块]</summary>
```

`<analysis>` 是给模型的「思考空间」，有它摘要质量更高。

**9 个固定章节**:① 主要请求和意图 ② 关键技术概念 ③ 涉及的文件和代码段 ④ 错误和修复 ⑤ 解决的问题 ⑥ **所有用户消息** ⑦ 待办任务 ⑧ **当前工作** ⑨ 下一步建议。

**两个灵魂章节**:
- **第 6 项：枚举所有用户消息**——不是概括，是枚举，一个不能落。用户在第 30/80/150 轮改的需求、提的约束、放弃的方向，是任务方向变化的关键信号，丢一个就走偏；
- **第 8 项：最细颗粒度的当前进度**——不是「正在调试」，而是「正在调试登录模块的 token 刷新逻辑，刚发现 cookie 过期判断有 bug，正准备改 auth.ts 的 refreshToken 函数」。越细 agent 接续越流畅，粗了会重新探索浪费 token。

### 摘要用什么模型？（反直觉）

**用当前对话的同一个模型，不省钱换小模型。** 两个原因：①摘要质量是 agent 接续工作的「灵魂文件」，小模型丢东西接不上；②**复用 Prompt Cache**——同模型能复用 system prompt 部分的缓存，省的钱比换小模型还多。

---

## 八、压完之后怎么接续对话

**五步流水线**:① 全量消息送摘要器 → ② 清空缓存（readFileState / loadedNestedMemoryPaths / getUserContext)→ ③ **并发**生成附件（最近文件、异步 agent 状态、技能）→ ④ `buildPostCompactMessages` 组装新消息链 → ⑤ 新链替换旧链，对话继续。用户只看到一句轻飘飘的「Compacted」。

**旧消息真丢了**：压缩是一次性、破坏性的操作（除非开 Kairos 模式把旧消息备份到本地 transcript 文件）。允许回滚会让对话状态复杂且省不下 token，一刀切反而干净。

**两个小心思**:
1. 摘要开头包装一句：「**本会话是从之前一次因上下文耗尽而中断的对话延续过来的**」——告诉模型你是接力不是从头开始，直接顺着 Current Work 接活；
2. 摘要末尾带 transcript 文件路径——给 agent 留「翻底牌」的兜底通道，需要细节可以自己去读。

---

## 九、面试速答模板（四层结构，1.5~2 分钟）

**开场一句**:「Claude Code 用的不是滑动窗口、定期摘要、向量召回那类常见方案，而是『全量重写 + 分通道恢复』的工程化思路。」

**第一层·触发时机**：绝对 token 阈值，「有效上下文窗口 - 13k 缓冲」。细节：有效窗口本身已先从原始窗口抠掉约 20k 给摘要输出（依据 p99.99 实测 17,387 token 向上取整）,13k 是其上第二道独立安全线——离原始窗口实际约 33k。

**第二层·取舍逻辑**：不保留最近 N 条，所有历史一刀切送摘要器重写；状态信息走附件通道（最近 5 个文件、每个 ≤5k、总预算 ≤50k；异步任务状态；计划文件）;CLAUDE.md 不进摘要，清空 getUserContext 缓存让下轮自动重载；system prompt 压缩后重建顺便刷新工具列表。

**第三层·摘要 prompt 设计**:200 多行，开头结尾两遍「禁止工具调用」;XML 格式（analysis 草稿区 + summary 正式区）;9 部分结构化清单，重点是「所有用户消息必须枚举不能落」「当前工作精确到文件和函数名」；摘要用当前对话同一模型，保质量 + 复用 prompt cache。

**第四层·接续机制**：清空文件状态缓存、并发生成附件、buildPostCompactMessages 组装；摘要外包一句「这是从上下文耗尽中断的对话延续过来的」;自动触发时开 suppressFollowUpQuestions 避免摘要塞问题打断任务；连续失败 3 次熔断，querySource 递归守卫防摘要任务自己再触发压缩。

**收口一句**:「这套设计反映的不是『省 token』的小聪明，而是『信息分通道管理』的工程哲学——不同信息有不同的半衰期，要分别管理。」

---

## 十、开放问题（面试官追问储备)

**「窗口扩到 1 亿 token，还需要 compaction 吗?」** → 仍然需要。Lost in the Middle 是注意力机制的固有特性，与窗口大小无关；窗口再大，中间段照样看不清。主动管理信息结构永远不会过时。

---

## 关键数字速查

| 数字 | 含义 |
|---|---|
| 5k~10k | 开局固定开支（system prompt + 工具描述 + CLAUDE.md) |
| 50KB / 2KB / 200KB | 单工具结果存磁盘阈值 / 预览大小 / 单消息工具结果总量上限 |
| 60 分钟 | Micro-Compact 时间衰减触发（Prompt Cache 过期） |
| 最近 5 个 | Micro-Compact 保留的可重获取工具结果数 |
| 90% / 95% | Context Collapse 触发 / 升级阈值 |
| 17,387 / 20k / 13k / 33k | 摘要输出 p99.99 实测 / 摘要输出预留 / 额外安全线 / 离原始窗口总缓冲 |
| 3 次 | auto-compact 连续失败熔断阈值 |
| 5 个 / 5K / 50K | 压缩后恢复文件数 / 单文件 token / 总预算 |
| 9 部分 | 摘要 prompt 的结构化清单数 |
