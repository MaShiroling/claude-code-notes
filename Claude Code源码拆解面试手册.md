# Claude Code 源码拆解面试手册

> 整理自《Claude Code 源码拆解：51 万行泄漏代码里的架构设计》(公众号 @小林 coding)
> 背景：Claude Code 因 npm 打包时误传 .map 文件导致 51 万行**客户端**源码泄漏（注意：不是大模型源码）。

---

## 〇、一句话总纲（电梯版）

- **Harness Engineering（线束工程）**:AI 圈风向已从「拼模型智商」转向「拼系统求稳」——与其祈求模型不产生幻觉，不如用系统约束它的行为。Claude Code 源码 80% 的代码不是让 AI 更聪明，而是在死磕**可靠性**。
- **核心哲学**：模型是发动机，但能不能安全上路靠的是刹车、方向盘、安全带。
- 做 Agent 的一句话心法：**「信任模型的推理能力，把应用层框架做得尽可能简单。」**

---

## 一、Claude Code 是什么

- Anthropic 官方编程 Agent，在终端里读代码、改文件、跑命令、管理 Git。
- **Agent vs ChatBot vs Copilot**（高频概念题）:
  - ChatBot：一问一答，一次性；
  - Copilot：代码补全，一次性预测；
  - Agent:**「感知-决策-行动」自主循环**——给定目标后自主决定读哪个文件、跑什么命令、改哪行代码，可循环几十轮直到完成。关键：**大模型自己决定下一步做什么**，不是按预定义流程图走。

---

## 二、四层架构（架构题必考）

| 层 | 职责 | 关键设计 |
|---|---|---|
| **引擎层** | Agent 的「大脑」：协调（拼装上下文发 API)、分发（找到并执行工具）、决策（继续循环 or 结束） | **不含任何业务逻辑**；新增能力只需新增工具，引擎层不用改 |
| **工具层** | 40+ 个工具（Shell、读写文件、搜索、子 Agent) | 统一规范 + **类型系统强制**三个安全属性：是否只读？是否有破坏性需确认？能否并发？漏一个就编译不过——「每把刀出厂就配好刀鞘」 |
| **服务层** | 共享基础设施：大模型 API 调用、上下文压缩、MCP 协议 | 类比大楼的水电煤 |
| **安全与治理层** | 像一张网罩在所有层之上：权限系统、Hook 系统（工具执行前后插自定义行为）、Bash 安全模块 | Bash 安全做**语法级分析**检测命令注入/路径逃逸，不是正则匹配关键词 |

---

## 三、Agent 工作模式：Tool-Use Loop（为什么不用 ReAct)

### ReAct(2022）的问题

1. **Token 浪费**：每轮输出 Thought 文本，50 轮循环就是几万 token 浪费；
2. **应用层复杂**：要解析模型输出区分 Thought/Action/Observation，格式不标准就全崩；
3. **为弱模型设计**:GPT-3.5 时代需要显式 Thought 引导，Opus 级模型可在内部完成推理。

### Claude Code 的方案：Tool-Use Loop

核心就是一个 `while(true)` 循环（`query.ts`)，模型只返回两种结果：

- `tool_use` → 应用层执行工具，结果拼入消息列表，`continue`;
- `end_turn` → `break`，返回最终结果。

### 为什么更好（三个原因）

1. **Extended Thinking** 让推理在模型内部完成，不占应用上下文、不需要应用层解析；
2. API **原生支持 tool_use**，不需要正则从文本里提取 Action;
3. **end_turn 是天然的终止信号**,API 层原语，无需解析「Final Answer」。

| 维度 | ReAct | Tool-Use Loop |
|---|---|---|
| 推理方式 | 显式 Thought 文本 | 模型内部 Extended Thinking |
| 工具调用 | 解析文本提取 Action | API 原生 tool_use |
| 终止判断 | 检测「Final Answer」 | API 原生 end_turn |
| Token 开销 | 每轮输出 Thought | 无额外开销 |
| 编排复杂度 | 高 | 低（if/else) |
| 适合场景 | 弱模型+简单工具 | 强模型+复杂工具集 |

### Plan Mode（两阶段工作流）

- 不是独立框架，就是同一循环里的 `EnterPlanMode` / `ExitPlanMode` **两个工具**——「工具即能力」，引擎层无需特殊处理。
- 三步流程：①模型判断任务复杂自主进入（或用户 Shift+Tab);②**权限降为只读**，只能用 Read/Grep/Glob 探索，计划写入 `.claude/plans/`，每 5 轮塞「小纸条」提醒别手痒改代码；③`ExitPlanMode` 需**用户审批**后恢复权限执行。

---

## 四、System Prompt 的构造（Prompt 工程题）

**不是静态文本，是十几个 Section 动态组装 + 精巧的缓存优化。**

### 值得抄作业的 Prompt 设计

1. **角色定位**：自称「interactive agent」而非 assistant/chatbot——暗示主动行动；立刻划红线「不能乱编 URL」。
2. **安全约束「先肯定再约束」**：先说可以做什么（授权安全测试、CTF、教育场景），再说不能做什么（DoS、供应链攻击）。给模型判断依据，而非模糊禁令。
3. **行为准则**:
   - 改代码前必须先读代码（防止与项目风格不一致/重复实现）;
   - 「少即是多」：不做用户没要求的重构、「改进」、抽象——三行相似代码好过一个过早的抽象；
   - 失败处理「先诊断再换方案」：读报错、检查假设、有针对性修复；不盲目重试，也不一次失败就放弃。
4. **操作安全 = 可逆性（reversibility)× 影响范围（blast radius）两个维度**:
   - 本地可逆操作（编辑文件、跑测试）→ 直接放行；
   - 不可逆/影响他人（git push、删表、发消息、上传第三方）→ 必须用户确认；
   - **授权一次性、有范围**：批准一次 push 不代表永远批准（防「权限蔓延」)。
5. **工具使用指南**：有专用工具就不用 Bash(Read 而非 cat、Edit 而非 sed、Glob 而非 find、Grep 而非 grep)。原因：**可审查性**(UI 清晰展示行为）+ **专用工具有专用权限检查**。
6. **Git 安全协议**：绝不改 git config、绝不跑破坏性命令、绝不 --no-verify、绝不 force push 到 main;**关键：永远新建 commit 而不用 --amend**——因为 pre-commit hook 失败时 commit 根本没发生，--amend 会改到上一个不相关的 commit 导致代码丢失。
7. **输出风格**：工具调用之间不超过 25 个词，最终回复不超过 100 个词——防 Agent「话痨」。
8. **环境信息注入**：工作目录、是否 git 仓库、OS 平台、Shell 类型、当前模型、知识截止日期。

### 分割线与三级缓存（成本优化，高频亮点题）

```
[角色定义][安全红线][行为准则][操作安全][工具使用][Git安全][输出风格]  ← 所有用户完全一样
────────── __SYSTEM_PROMPT_DYNAMIC_BOUNDARY__ ──────────
[环境信息][CLAUDE.md][记忆指令][MCP 指令]                              ← 每个用户/项目不一样
```

- 原因：Claude API 的 **Prompt Cache**——两次请求 Prompt 前缀相同就复用计算结果，**费用降 90%**。
- 分割线之上全球所有用户共享同一份缓存；之下因人而异实时生成。
- **三级缓存**：全局缓存（跨组织跨用户）→ 组织缓存（同组织跨会话）→ 会话缓存（同 Section 会话内只算一次）。
- 小结：System Prompt 的本质是**用最小的 token 成本，给模型划出最清晰的行为边界**。

---

## 五、记忆系统（为什么不用向量数据库）

**业界方案**：向量数据库 + embedding 相似度检索。**Claude Code 不用**，因为 Agent 要记的多是「用户说过不要 mock 数据库」这种**结构化行为指令**，向量检索会匹配一堆含「数据库」关键词的无关内容。

### 记什么：四种类型（封闭集合，不可新增）

| 类型 | 内容 | 细节 |
|---|---|---|
| `user` | 用户画像：角色、偏好、知识水平 | 让回答因人而异 |
| `feedback` | 行为反馈（**最重要**) | 必须同时记 **Why**（为什么）和 **How to apply**（怎么应用）——没 Why 只能盲目遵守，无法应对边缘情况 |
| `project` | 项目动态：在做什么、截止日期 | **相对日期必须转绝对日期**(「周四前冻结」→「2026-03-05 前冻结」) |
| `reference` | 外部指针：去哪找什么信息 | Agent 只需知道去哪找，不需知道具体内容 |

### 不记什么：排除清单（同等重要！)

代码模式/架构/文件结构、Git 历史、调试方案、CLAUDE.md 已有内容、临时任务状态——**一律不存**。
核心原则：**可以从当前代码推导出来的信息一律不存**。代码是活的、记忆是死的，过时的记忆是「权威的错误」，比没有记忆更糟。

### 怎么存：索引 + 独立文件

- 每条记忆 = 一个独立 `.md` 文件 + YAML frontmatter(name / description / type);
- `MEMORY.md` 作为**轻量索引始终加载进 System Prompt**，上限 **200 行 / 25KB**（行数+字节数双重检查，防 199 行×500 字的漏洞）;
- 独立文件**按需加载**——索引让 Agent 知道有什么，又不撑爆上下文。

### 怎么召回：Sonnet 当秘书（三步）

1. 扫描所有记忆文件**只读前 30 行**(frontmatter)，按修改时间倒序最多 200 个；
2. 拼成清单 + 用户当前输入，发给**便宜的 Sonnet**(max_tokens=256)，返回最多 5 个文件名（不返回内容）;
3. 只加载选中记忆的完整内容，以 `<system-reminder>` 注入上下文。

**两个精妙细节**:

- **陈旧度检测**：超过 1 天的记忆自动附加警告「信息可能过时，先对照当前代码验证再引用」;
- **并行预取**:Sonnet 选择在用户提交消息后立刻开始，与主模型 API 调用**并行**,Sonnet 只要几百毫秒，等 Opus 响应回来时选择早已完成——**零额外延迟**。
- 小优化：正在用某工具时，过滤掉该工具的「使用文档」类记忆（噪声），但保留「已知 bug/注意事项」类记忆（恰恰是信号）。

### 小结三句话

1. 记该记的，不记能推导的；
2. 存索引，按需加载详情；
3. 用小模型做秘书，大模型做决策。

---

## 六、上下文窗口管理：五步压缩（最复杂最精妙的部分）

**核心理念：压缩一定有信息损失，所以能不压就不压，必须压就从最轻的手段开始。**（类比医院分诊）

| 层 | 手段 | 信息损失 | API 开销 | 触发条件 |
|---|---|---|---|---|
| 1 | 大结果存磁盘 | 几乎为零 | 零 | 单个工具结果 > 50KB |
| 2 | Snip 砍远古消息 | 低 | 零 | 消息过时 |
| 3 | Micro-Compact 清理老工具输出 | 中低 | 零 | 缓存过期/数量超限 |
| 4 | Context Collapse 读时投影 | 中 | 低 | 上下文达 90%/95% |
| 5 | Auto-Compact 全量摘要 | 高 | 高（一次 API 调用） | 上下文达 ~93% |

**逐层细节**:

1. **大结果存磁盘**：单个工具结果 >50KB → 完整内容写磁盘，消息里只留 2KB 预览；另有消息级总量控制（同一条消息所有工具结果 ≤200KB)。完整内容没丢，模型需要时可再调 Read 读特定行范围。
2. **Snip 砍远古消息**：直接删掉对话开头的老消息 + 插入边界标记，零 API 开销。会把 `snipTokensFreed` 传给第 5 层，避免重复压缩（层间协调）。
3. **Micro-Compact 时间衰减**：只裁剪**可重新获取**的工具结果（Read/Bash/Grep/Glob/WebSearch/Edit/Write),**保留最近 N 个**、其余替换为 `[Old tool result content cleared]` 标记；**AgentTool/TaskTool 的结果永不裁剪**（子 Agent 推理不可重复）。触发与 ~60 分钟 Prompt Cache 过期时间挂钩。
4. **Context Collapse 读时投影（Read-Time Projection)**:**不修改原始消息**，只在调 API 那一刻动态计算「压缩视图」;90% 阈值主动压缩、95% 紧急压缩；在第 5 层之前运行，它够用第 5 层就不触发。
5. **Auto-Compact 全量摘要**：阈值公式 = 有效窗口（200K-20K 输出预留 ≈180K) - 13K 缓冲区 ≈ **167K 触发**。三步走：
   - 生成结构化摘要（用户意图/技术概念/涉及文件/错误与修复/所有用户消息不遗漏/待办/当前状态/下一步）;
   - 替换旧消息为摘要 + 边界标记；
   - **Post-Compact Restoration（压缩后恢复）**：从 fileStateCache 恢复最近访问的最多 5 个文件（≤50K token)、活跃 Skill(≤25K)、进行中的 Plan——防止模型「失忆」后白白重新读文件。
   - **熔断器**：全量摘要连续失败 3 次自动放弃，不无限重试。

---

## 七、面试速答模板

**问：你了解 Claude Code 的架构吗？**

> 「Claude Code 是四层架构：引擎层只做协调、分发、决策，不含业务逻辑；工具层 40 多个工具，类型系统强制只读/破坏性/并发三个安全属性；服务层提供 API 调用、上下文压缩、MCP；安全与治理层像网一样罩在上面。它的 Agent 循环没用 ReAct，而是一个极简的 Tool-Use Loop——信任模型用 Extended Thinking 内部推理，API 原生 tool_use 和 end_turn 分别驱动继续和终止。记忆系统不用向量数据库，而是 user/feedback/project/reference 四类型 + MEMORY.md 索引 + Sonnet 并行预取召回。上下文管理是五步从轻到重的压缩：大结果存磁盘、Snip、Micro-Compact、读时投影、全量摘要+压缩后恢复。整体哲学是 Harness Engineering——80% 的代码在死磕可靠性而不是模型智商。」

**问：你自己做 Agent 会借鉴什么？（三选一说透即可）**

1. System Prompt 的**动态分割线 + Prompt Cache**（省 90% 费用）;
2. 记忆的**四类型封闭集合 + 排除清单**（可推导的一律不存）;
3. 压缩的**分层渐进策略**（能轻则轻，每层为下一层减负）。

---

## 八、关键数字速查表

| 数字 | 含义 |
|---|---|
| 51 万行 | 泄漏的客户端源码规模 |
| 40+ | 工具数量 |
| 25 词 / 100 词 | 工具调用间文字 / 最终回复上限 |
| 90% | Prompt Cache 命中节省的费用 |
| 4 种 | 记忆类型（user/feedback/project/reference) |
| 200 行 / 25KB | MEMORY.md 索引上限（行数+字节双检查） |
| 前 30 行 | 记忆召回时每个文件只读的 frontmatter 范围 |
| ≤5 条 / 256 tokens | Sonnet 每次召回的记忆条数上限 / 响应长度 |
| 1 天 | 记忆陈旧度警告阈值 |
| 50KB / 200KB | 单工具结果 / 单消息工具结果总量阈值 |
| 2KB | 大结果存磁盘后的预览大小 |
| ~60 分钟 | Micro-Compact 触发相关的 Prompt Cache 过期时间 |
| 90% / 95% | Context Collapse 两级阈值 |
| 167K | 200K 窗口模型的 Auto-Compact 触发点（180K 有效窗口 - 13K 缓冲） |
| 5 个 / 50K / 25K | 压缩后恢复的文件数 / 文件 token 预算 / Skill token 预算 |
| 3 次 | 全量摘要连续失败的熔断阈值 |
