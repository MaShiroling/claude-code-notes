# Claude Code 代码检索面试手册

> 整理自《Claude Code 代码检索图解：为什么用 grep 而不用 RAG？》(公众号 @小林 coding)
> 面试真题（字节 AI Agent 岗):「为什么 Claude Code 不用 RAG 检索代码，而是直接用 grep?」

---

## 〇、三句话精炼版答案（先背这个)

> Claude Code 不用 RAG 基于三层考虑：
> **第一**，代码场景下 RAG 有切片破坏结构、向量近似不准、索引滞后等本质问题；
> **第二**,Claude Code 用 Grep + Glob + Read 三件套加派子 agent 探索，本质是把**检索决策权还给 LLM 自己**，配合多轮迭代循环实现精准定位；
> **第三**，更深层是 Anthropic 信任 LLM 能力、押注模型越来越强，选择「**不替模型做决定**」的设计哲学。
>
> 这道题表面问技术选型，实际问的是你对 Agent 设计哲学的理解。

---

## 一、代码检索要解决什么问题

- 模型不能直接「看」磁盘代码，只能看塞进上下文的文字；整个项目几百万行远超窗口（1M token ≈ 200 万字也不够），所以必须**精准捞出相关片段**再塞给模型。
- 类比查字典：不从头读到尾，先翻目录索引定位那一页，只读那一页。
- RAG 的核心套路：把「找代码」转成「算相似度」——向量空间里找最近邻。
- RAG 四步流程：**切片（Chunking)→ 向量化（Embedding)→ 建索引（Indexing,Faiss/Pinecone/Milvus)→ 召回（Retrieval,Top-K)**。适合「静态文档、自然语言、模糊匹配」场景（如客服 FAQ)。

---

## 二、RAG 在代码场景的五大痛点（水土不服)

| 痛点 | 说明 |
|---|---|
| 1. 代码切不动 | 代码有严格结构：函数按 100 行切开，一段少 if 一段少 else；函数 A 调 B 却分属不同片段 → 幻觉拉满 |
| 2. 精确匹配干不了 | 向量找「相似」不是找「对」：要 `getUserById`，召回来 `getUserByName`/`fetchUserInfo` 一堆（叫张三结果李四王五全来了） |
| 3. 代码每天在变，索引咋办 | 重建贵、不重建用过期信息写新代码、增量更新一堆边界情况——简单事情搞复杂 |
| 4. 冷启动慢 | 百万行代码库建索引十几分钟；Claude Code 理念是「打开就能用」 |
| 5. 黑盒不可解释 | Top-K 为什么是这 5 个没人答得上；答错了分不清是召回错还是理解错，无法 debug |

**总结**：代码是**动态、结构化、需要精确**的，恰好和 RAG 的主场相反。

---

## 三、Claude Code 的反向思路：把检索还给模型自己

程序员自己怎么找代码？**「找文件 → 找内容 → 看具体代码」循环往复，现用现找，从不提前建索引。**

Claude Code 的哲学一模一样——不预处理、不建库、不算向量，工具就给三个：

| 工具 | 对应程序员动作 | 关键设计 |
|---|---|---|
| **Glob** | find，按文件名 pattern 找文件 | 结果**按修改时间倒序**（最近改的≈最相关）;**100 文件硬上限**防输出爆炸 |
| **Grep** | grep -r，按内容关键字找 | 底层 **ripgrep**(Rust 多线程、自动尊重 .gitignore)；三种输出粒度：content（行号+内容）/ files_with_matches（默认，只给文件名）/ count（只给数量） |
| **Read** | cat/编辑器跳转，读文件 | **默认只读 2000 行**，超出截断；支持 offset/limit 分段精准读；**每次直接 stat 磁盘读最新内容，不缓存不索引**——实时性的来源 |

**为什么有 Bash 还要单独包 Grep 工具？（三层考虑）**
1. **权限统一管控**:Bash 万能（能 rm/curl/git push)，高频操作单独画权限闸门更安全；
2. **输出格式可控**：结构化输出、三种粒度按需选，省 token;
3. **性能**:ripgrep 甩老牌 grep 几条街。

System prompt 里语气强硬:「ALWAYS use Grep ... NEVER invoke grep or rg as a Bash command」——强制走专用工具，不许 Bash 抄近路（可审查性 + 权限检查）。

**三件套组合工作流**（例：「登录功能在哪?」):
① Glob `**/*login*.{ts,tsx,js}` 拉候选 → ② Grep `passport|auth|login` 定位命中行 → ③ Read 读相关行段。**每一步基于上一步结果调整方向**——这是和 RAG 一次性召回最本质的不同。

---

## 四、三件套不够用：派子 agent 去探索（上下文隔离)

**问题**：调研类任务要 grep/read 十几次，主 agent 上下文被中间结果塞满——**上下文污染**，真正要解决的问题被挤到角落。

**方案：Explore 子 agent**（老板派秘书调研，只看精简报告）:
1. 主 agent 通过 **Agent 工具**派子 agent，子 agent 是独立实例，上下文完全隔离；
2. 子 agent 拿**精简只读工具池**(Grep/Glob/Read/只读 Bash),**不能 Edit/Write/再派子 agent**（防嵌套递归）;
3. 子 agent 在自己上下文里多轮迭代，中间结果不外泄；
4. **只把最终结论返回主 agent**——脏活子 agent 干，主 agent 只收干净结论。

**工程经验（源码 prompt 明确写了）**：简单定向搜索直接用三件套；开放式探索用 `subagent_type=Explore`;**临界点是「预期超过 3 次查询」**——少于 3 次别折腾，多于 3 次别污染主上下文。

**加分项：子 agent 可并行派多个**（认证/支付/订单三个模块同时调研）。

**层次感**：底层三件套（定向检索）→ 中层 Explore 子 agent（开放探索+上下文隔离）→ 上层主 agent 编排。

---

## 五、最深层：LLM-driven 多轮迭代循环（Agent 范式的灵魂)

| | RAG 范式 | Claude Code 范式 |
|---|---|---|
| 类比 | 考试发卷子 | 现场探案 |
| 流程 | 提问 → 一次性召回 Top-K → 一次性生成答案 | 提问 → Grep → 看结果 → Read → 看内容 → 调整方向 → …… 直到搞定 |
| 出错时 | 召回错了只能将错就错 | 下一轮自己调整（换关键词、换文件） |

核心就是 `query.ts` 的 while 循环：模型说话 → 可能带 tool_use → 执行工具 → 结果回填 → 模型继续。**给了模型每一步根据上一步结果调整方向的能力。**

> grep 本身不强，但「让 LLM 自己决定每一轮 grep 什么」就强了。

---

## 六、为什么不用 RAG:六大原因 + 两种哲学

**六大原因**:
1. **冷启动**:grep 毫秒级开箱即用;RAG 建索引分钟级，劝退用户；
2. **实时性**:grep 现读磁盘最新版；RAG 索引滞后需重建；
3. **精确性**:grep 确定性字符匹配，找 `getUserById` 就只有它;RAG 向量近似把一堆相似函数糊在一起；
4. **Token 经济**:grep+Read 按需读取只看几行;RAG 要给整个代码库做 embedding，存储计算成本都不小；
5. **可解释性**:grep 每步透明可审计;RAG Top-K 是黑盒；
6. **决策权**:grep 让 LLM 自己决定每轮搜什么，多轮迭代逼近;RAG 一次性丢材料，模型将错就错。

**两种设计哲学（升华层）**:
- RAG 派：「LLM 不够强，用工程手段替它把材料准备好」——chunking/embedding/召回都是**替模型做决定**;
- Claude Code 派：「LLM 已足够强，工程的角色是备好工具，**把决策权还给它**」——Anthropic 押注模型会越来越强，这是长期主义选择。

---

## 七、RAG 该被淘汰吗？（辩证收尾，防面试官反问)

RAG 仍有舞台：
1. **巨型代码库 + 跨仓库检索**（几十个 monorepo、上千万行，grep 性能扛不住）;
2. **纯语义查询**（「找处理用户认证相关的代码」这种模糊描述，关键字反而不好搜）;
3. **多人协作知识库混合检索**（代码+文档+Wiki)。

Claude Code 方案最适合：**单项目、探索式开发、需要精确性、要求实时性**——恰好是大部分 AI 编程工具的主战场。**工具是为场景服务的，没有银弹。**

开放问题：窗口到 1 亿 token 后 RAG/grep 还有意义吗？（留作面试官追问的讨论引子）

---

## 八、收尾金句

> Agent 不是带工具的聊天机器人，而是会自己做决策的执行体。**工程师的职责是给它一套好用的工具，而不是替它做决策。**

---

## 关键数字速查

| 数字 | 含义 |
|---|---|
| 2000 行 | Read 默认读取上限（超出用 offset/limit 分段） |
| 100 个 | Glob 结果文件数硬上限 |
| 3 次查询 | 直接检索 vs 派 Explore 子 agent 的临界点 |
| 3 种 | Grep 输出模式：content / files_with_matches / count |
| 4 步 | RAG 流程：切片 → 向量化 → 建索引 → 召回 |
